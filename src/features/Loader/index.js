import { Wrapper } from "../../common";

const Loader = () => (
    <Wrapper>
        <h2>Search results for "Mulan"</h2>
        <div>
            <div></div>
        </div>
    </Wrapper>
);

export default Loader;